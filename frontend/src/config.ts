/**
 * Slug de demonstração utilizado durante o desenvolvimento local quando o
 * serviço é acessado diretamente pela raiz ('/') sem um domínio customizado.
 *
 * Deve refletir o tenant de seed criado pelo backend (`npm run seed`).
 */
export const DEV_DEFAULT_SLUG = 'studiofy-demo';

/** Nome exibido na página pública de erro/fallback do domínio. */
export const APP_NAME = 'StudioFy';
